# X post copy — VidLens V2 trailer

Release: `v2-x-launch` · Platform: X · Refreshed 2026-08-17 · **Not posted.**

All three earlier blockers are resolved. The video at `../video/v3-trailer-final.mp4`
passes every X check (15.04 s, 1080p24 H.264/AAC, -14.5 LUFS). The end card reads
"VidLens V2 — Coming Soon" with the locked tagline. Pick one option below, attach the
video, paste the alt text, post.

Voice source: brandbook §11 — plain product truth, concrete nouns, no "AI-powered".
Locked tagline, verbatim in all three:

> Your AI can read the web. Now it can also watch it.

The `[video]` line is a staging marker for the attachment, not text you type.
An attached video does not consume characters on X.

---

## Primary — A (247 chars posted)

```
Your AI can read the web. Now it can also watch it.

VidLens V2 is an MCP server that gives your agent eyes on video. YouTube, TikTok, X, or a file on your disk. Transcripts, frames, embeddings, kept in a local library it can search.

Coming soon.

[video]
```

Why: the tagline gets the first line to itself, so the quote-tweet crop and the
timeline preview both lead on it. Room for `#MCP` if wanted.

## Variant B — contrast open (266 chars posted)

```
Most tools hear a video. VidLens sees it.

V2 is an MCP server: hand your agent a YouTube, TikTok, X, or local video and it gets transcripts, frames, embeddings, and a local library to search them in.

Your AI can read the web. Now it can also watch it. Coming soon.

[video]
```

Why: sharpest hook for a cold timeline. No room for a hashtag.

## Variant C — shortest (234 chars posted)

```
Your AI can read the web. Now it can also watch it.

VidLens V2: an MCP server. Video in, transcripts and frames and embeddings out, into a library on your own disk your agent can search. YouTube, TikTok, X, local files.

Coming soon.

[video]
```

Why: "on your own disk" answers the "where does my data go" question inside the
announcement instead of in the replies. Most hashtag headroom.

---

## Hashtag

One or none. If one: `#MCP` on its own line (+5 chars). Fits A and C, not B.
Do not use `#AI`, `#buildinpublic`, or `#opensource`.

## Alt text (paste onto the video when uploading)

```
Watercolor animation on handmade paper: an indigo camera aperture, ringed in
gold, connects source icons (play button, bird, music note, film reel) to a
terminal and a phone. Text: "VidLens V2 — Coming Soon. YouTube, TikTok, X,
Local → VidLens MCP → your AI agent. Your AI can read the web. Now it can
also watch it."
```

## Still open — your call

- [ ] Pick post copy: A / B / C
- [ ] Hashtag: `#MCP` or none
- [ ] Upload window: see `../video/platform-spec.md` (researched windows W1–W5)
- [ ] Post the article before, after, or same day as the video post
      (draft: `../article/x-article.md`)

The upload button is yours. Nothing here has been posted, uploaded, or scheduled.
